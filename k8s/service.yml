apiVersion: v1
kind: Service

metadata:
  name: flipkart-service
  namespace: default

spec:
  type: LoadBalancer

  selector:
    app: flipkart-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
